import Subtract from './difference.js'; // default export can be imported with any name

const a = 10;
const b = 5;
const result = Subtract(a, b);
console.log(`The difference between ${a} and ${b} is: ${result}`);